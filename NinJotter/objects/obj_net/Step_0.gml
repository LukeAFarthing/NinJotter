/*if(instance_place(x,y,obj_player)){
	instance_destroy(self)
	if(obj_fisher_king.image_xscale==-1){
	obj_player.x= obj_fisher_king.x-150;
	}else{
		obj_player.x= obj_fisher_king.x+150;
	}
}*/	

// I also tried to make the net draw the player to the fisher king but it won't work because it is repeating the same code every frame. 
